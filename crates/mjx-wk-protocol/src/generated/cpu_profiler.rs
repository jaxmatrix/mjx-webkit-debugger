// @generated by `cargo run -p xtask -- codegen`. DO NOT EDIT BY HAND.
//
// Regenerate after updating `reference/webkit-protocol/` (git-ignored; see
// reference/README.md). `cargo run -p xtask -- verify-protocol` checks this
// output against the protocol the installed WebKit actually exposes.

//! The `CPUProfiler` domain.
//!
//! CPUProfiler domain exposes cpu usage tracking.
//!
//! Debuggable types: `web-page`
//! Target types: `page`

use serde::{Deserialize, Serialize};

/// CPU usage for an individual thread.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct ThreadInfo {
    /// Some thread identification information.
    pub name: String,
    /// CPU usage for this thread. This should not exceed 100% for an individual thread.
    pub usage: f64,
    /// Type of thread. There should be a single main thread.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<crate::generated::cpu_profiler::ThreadInfoType>,
    /// A thread may be associated with a target, such as a Worker, in the process.
    #[serde(rename = "targetId", default, skip_serializing_if = "Option::is_none")]
    pub target_id: Option<String>,
}

/// Undocumented upstream.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Event {
    pub timestamp: f64,
    /// Percent of total cpu usage. If there are multiple cores the usage may be greater than 100%.
    pub usage: f64,
    /// Per-thread CPU usage information. Does not include the main thread.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub threads: Option<Vec<crate::generated::cpu_profiler::ThreadInfo>>,
}

/// The values `CPUProfiler.ThreadInfo` accepts.
///
/// Synthesised: the protocol declares this set inline rather than naming it.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum ThreadInfoType {
    #[serde(rename = "main")]
    Main,
    #[serde(rename = "webkit")]
    Webkit,
}

/// Requests the debugger can send to a `CPUProfiler` debuggee.
pub mod commands {
    use serde::{Deserialize, Serialize};

    /// Start tracking cpu usage.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct StartTracking {}

    impl crate::Command for StartTracking {
        const DOMAIN: crate::Domain = crate::Domain::CpuProfiler;
        const METHOD: &'static str = "startTracking";
        type Returns = crate::generated::Empty;
    }

    /// Stop tracking cpu usage. This will produce a `trackingComplete` event.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct StopTracking {}

    impl crate::Command for StopTracking {
        const DOMAIN: crate::Domain = crate::Domain::CpuProfiler;
        const METHOD: &'static str = "stopTracking";
        type Returns = crate::generated::Empty;
    }
}

/// Messages a `CPUProfiler` debuggee sends unprompted.
pub mod events {
    use serde::{Deserialize, Serialize};

    /// Tracking started.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct TrackingStart {
        pub timestamp: f64,
    }

    impl crate::Event for TrackingStart {
        const DOMAIN: crate::Domain = crate::Domain::CpuProfiler;
        const METHOD: &'static str = "trackingStart";
    }

    /// Periodic tracking updates with event data.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct TrackingUpdate {
        pub event: crate::generated::cpu_profiler::Event,
    }

    impl crate::Event for TrackingUpdate {
        const DOMAIN: crate::Domain = crate::Domain::CpuProfiler;
        const METHOD: &'static str = "trackingUpdate";
    }

    /// Tracking stopped.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct TrackingComplete {
        pub timestamp: f64,
    }

    impl crate::Event for TrackingComplete {
        const DOMAIN: crate::Domain = crate::Domain::CpuProfiler;
        const METHOD: &'static str = "trackingComplete";
    }
}
