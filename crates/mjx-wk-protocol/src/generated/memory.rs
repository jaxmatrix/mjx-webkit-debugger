// @generated by `cargo run -p xtask -- codegen`. DO NOT EDIT BY HAND.
//
// Regenerate after updating `reference/webkit-protocol/` (git-ignored; see
// reference/README.md). `cargo run -p xtask -- verify-protocol` checks this
// output against the protocol the installed WebKit actually exposes.

//! The `Memory` domain.
//!
//! Memory domain exposes page memory tracking.
//!
//! Debuggable types: `web-page`
//! Target types: `page`

use serde::{Deserialize, Serialize};

/// Undocumented upstream.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Event {
    pub timestamp: f64,
    /// Breakdown of memory in categories.
    pub categories: Vec<crate::generated::memory::CategoryData>,
}

/// Undocumented upstream.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct CategoryData {
    /// Category type.
    pub r#type: crate::generated::memory::CategoryDataType,
    /// Category size in bytes.
    pub size: f64,
}

/// The values `Memory.CategoryData` accepts.
///
/// Synthesised: the protocol declares this set inline rather than naming it.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum CategoryDataType {
    #[serde(rename = "javascript")]
    Javascript,
    #[serde(rename = "jit")]
    Jit,
    #[serde(rename = "images")]
    Images,
    #[serde(rename = "layers")]
    Layers,
    #[serde(rename = "page")]
    Page,
    #[serde(rename = "other")]
    Other,
}

/// The values `Memory.memoryPressure` accepts.
///
/// Synthesised: the protocol declares this set inline rather than naming it.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum MemoryPressureSeverity {
    #[serde(rename = "critical")]
    Critical,
    #[serde(rename = "non-critical")]
    NonCritical,
}

/// Requests the debugger can send to a `Memory` debuggee.
pub mod commands {
    use serde::{Deserialize, Serialize};

    /// Enables Memory domain events.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct Enable {}

    impl crate::Command for Enable {
        const DOMAIN: crate::Domain = crate::Domain::Memory;
        const METHOD: &'static str = "enable";
        type Returns = crate::generated::Empty;
    }

    /// Disables Memory domain events.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct Disable {}

    impl crate::Command for Disable {
        const DOMAIN: crate::Domain = crate::Domain::Memory;
        const METHOD: &'static str = "disable";
        type Returns = crate::generated::Empty;
    }

    /// Start tracking memory. This will produce a `trackingStart` event.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct StartTracking {}

    impl crate::Command for StartTracking {
        const DOMAIN: crate::Domain = crate::Domain::Memory;
        const METHOD: &'static str = "startTracking";
        type Returns = crate::generated::Empty;
    }

    /// Stop tracking memory. This will produce a `trackingComplete` event.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct StopTracking {}

    impl crate::Command for StopTracking {
        const DOMAIN: crate::Domain = crate::Domain::Memory;
        const METHOD: &'static str = "stopTracking";
        type Returns = crate::generated::Empty;
    }
}

/// Messages a `Memory` debuggee sends unprompted.
pub mod events {
    use serde::{Deserialize, Serialize};

    /// Memory pressure was encountered.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct MemoryPressure {
        pub timestamp: f64,
        /// The severity of the memory pressure event.
        pub severity: crate::generated::memory::MemoryPressureSeverity,
    }

    impl crate::Event for MemoryPressure {
        const DOMAIN: crate::Domain = crate::Domain::Memory;
        const METHOD: &'static str = "memoryPressure";
    }

    /// Tracking started.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct TrackingStart {
        pub timestamp: f64,
    }

    impl crate::Event for TrackingStart {
        const DOMAIN: crate::Domain = crate::Domain::Memory;
        const METHOD: &'static str = "trackingStart";
    }

    /// Periodic tracking updates with event data.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct TrackingUpdate {
        pub event: crate::generated::memory::Event,
    }

    impl crate::Event for TrackingUpdate {
        const DOMAIN: crate::Domain = crate::Domain::Memory;
        const METHOD: &'static str = "trackingUpdate";
    }

    /// Tracking stopped.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct TrackingComplete {
        pub timestamp: f64,
    }

    impl crate::Event for TrackingComplete {
        const DOMAIN: crate::Domain = crate::Domain::Memory;
        const METHOD: &'static str = "trackingComplete";
    }
}
