// @generated by `cargo run -p xtask -- codegen`. DO NOT EDIT BY HAND.
//
// Regenerate after updating `reference/webkit-protocol/` (git-ignored; see
// reference/README.md). `cargo run -p xtask -- verify-protocol` checks this
// output against the protocol the installed WebKit actually exposes.

//! Generated protocol domains.
//!
//! One module per domain. Types sit at each module's root because that is how
//! cross-domain references address them; commands and events have their own
//! namespaces so a type and a same-named event cannot collide.
//!
//! ```text
//! generated::debugger::Location            // a type
//! generated::debugger::commands::Resume    // a command
//! generated::debugger::events::Paused      // an event
//! ```

use serde::{Deserialize, Serialize};

/// `Animation` — Domain for tracking/modifying Web Animations, as well as CSS (declarative) animations and transitions.
pub mod animation;
/// The `Audit` domain.
pub mod audit;
/// `Browser` — The Browser domain contains commands and events related to getting information about the browser
pub mod browser;
/// `Canvas` — Canvas domain allows tracking of canvases that have an associated graphics context. Tracks canvases in the DOM and CSS canvases created with -webkit-canvas.
pub mod canvas;
/// `Console` — Console domain defines methods and events for interaction with the JavaScript console. Console collects messages created by means of the JavaScript Console API. One needs to enable this domain using `enable` command in order to start receiving the console messages. Browser collects messages issued while console domain is not enabled as well and reports them using `messageAdded` notification upon enabling.
pub mod console;
/// `CPUProfiler` — CPUProfiler domain exposes cpu usage tracking.
pub mod cpu_profiler;
/// `CSS` — This domain exposes CSS read/write operations. All CSS objects, like stylesheets, rules, and styles, have an associated `id` used in subsequent operations on the related object. Each object type has a specific `id` structure, and those are not interchangeable between objects of different kinds. CSS objects can be loaded using the `get*ForNode()` calls (which accept a DOM node id). Alternatively, a client can discover all the existing stylesheets with the `getAllStyleSheets()` method and subsequently load the required stylesheet contents using the `getStyleSheet\[Text\]()` methods.
pub mod css;
/// `Debugger` — Debugger domain exposes JavaScript debugging capabilities. It allows setting and removing breakpoints, stepping through execution, exploring stack traces, etc.
pub mod debugger;
/// `DOM` — This domain exposes DOM read/write operations. Each DOM Node is represented with its mirror object that has an `id`. This `id` can be used to get additional information on the Node, resolve it into the JavaScript object wrapper, etc. It is important that client receives DOM events only for the nodes that are known to the client. Backend keeps track of the nodes that were sent to the client and never sends the same node twice. It is client's responsibility to collect information about the nodes that were sent to the client.Note that `iframe` owner elements will return corresponding document elements as their child nodes.
pub mod dom;
/// `DOMDebugger` — DOM debugging allows setting breakpoints on particular DOM operations and events. JavaScript execution will stop on these operations as if there was a regular breakpoint set.
pub mod dom_debugger;
/// `DOMStorage` — Query and modify DOM storage.
pub mod dom_storage;
/// `GenericTypes` — Exposes generic types to be used by any domain.
pub mod generic_types;
/// `Heap` — Heap domain exposes JavaScript heap attributes and capabilities.
pub mod heap;
/// The `IndexedDB` domain.
pub mod indexed_db;
/// The `Inspector` domain.
pub mod inspector;
/// The `LayerTree` domain.
pub mod layer_tree;
/// `Memory` — Memory domain exposes page memory tracking.
pub mod memory;
/// `Network` — Network domain allows tracking network activities of the page. It exposes information about http, file, data and other requests and responses, their headers, bodies, timing, etc.
pub mod network;
/// `Page` — Actions and events related to the inspected page belong to the page domain.
pub mod page;
/// `Recording` — General types used for recordings of actions performed in the inspected page.
pub mod recording;
/// `Runtime` — Runtime domain exposes JavaScript runtime by means of remote evaluation and mirror objects. Evaluation results are returned as mirror object that expose object type, string representation and unique identifier that can be used for further object reference. Original objects are maintained in memory unless they are either explicitly released or are released along with the other objects in their object group.
pub mod runtime;
/// `ScriptProfiler` — Profiler domain exposes JavaScript evaluation timing and profiling.
pub mod script_profiler;
/// `Security` — Security domain allows the frontend to query for information relating to the security of the page (e.g. HTTPS info, TLS info, user activity, etc.).
pub mod security;
/// `ServiceWorker` — Actions and events related to the inspected service worker.
pub mod service_worker;
/// The `Target` domain.
pub mod target;
/// `Timeline` — Timeline provides its clients with instrumentation records that are generated during the page runtime. Timeline instrumentation can be started and stopped using corresponding commands. While timeline is started, it is generating timeline event records.
pub mod timeline;
/// The `Worker` domain.
pub mod worker;

/// The reply of a command that returns nothing.
///
/// Deserialises from anything, because a debuggee may answer a void command
/// with `{}`, with `null`, or with no `result` member at all — all three occur.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default, Serialize)]
pub struct Empty;

impl<'de> Deserialize<'de> for Empty {
    fn deserialize<D: serde::Deserializer<'de>>(d: D) -> Result<Self, D::Error> {
        serde::de::IgnoredAny::deserialize(d)?;
        Ok(Empty)
    }
}
