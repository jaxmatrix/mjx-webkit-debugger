// @generated by `cargo run -p xtask -- codegen`. DO NOT EDIT BY HAND.
//
// Regenerate after updating `reference/webkit-protocol/` (git-ignored; see
// reference/README.md). `cargo run -p xtask -- verify-protocol` checks this
// output against the protocol the installed WebKit actually exposes.

//! The `Worker` domain.
//!
//! Debuggable types: `web-page`
//! Target types: `page`, `worker`

/// Requests the debugger can send to a `Worker` debuggee.
pub mod commands {
    use serde::{Deserialize, Serialize};

    /// Enable Worker domain events.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct Enable {}

    impl crate::Command for Enable {
        const DOMAIN: crate::Domain = crate::Domain::Worker;
        const METHOD: &'static str = "enable";
        type Returns = crate::generated::Empty;
    }

    /// Disable Worker domain events.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct Disable {}

    impl crate::Command for Disable {
        const DOMAIN: crate::Domain = crate::Domain::Worker;
        const METHOD: &'static str = "disable";
        type Returns = crate::generated::Empty;
    }

    /// Sent after the frontend has sent all initialization messages and can resume this worker. This command is required to allow execution in the worker.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct Initialized {
        #[serde(rename = "workerId")]
        pub worker_id: String,
    }

    impl crate::Command for Initialized {
        const DOMAIN: crate::Domain = crate::Domain::Worker;
        const METHOD: &'static str = "initialized";
        type Returns = crate::generated::Empty;
    }

    /// Send an Inspector Protocol message to be dispatched to a Worker's agents.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct SendMessageToWorker {
        #[serde(rename = "workerId")]
        pub worker_id: String,
        /// JSON Inspector Protocol message (command) to be dispatched on the backend.
        pub message: String,
    }

    impl crate::Command for SendMessageToWorker {
        const DOMAIN: crate::Domain = crate::Domain::Worker;
        const METHOD: &'static str = "sendMessageToWorker";
        type Returns = crate::generated::Empty;
    }
}

/// Messages a `Worker` debuggee sends unprompted.
pub mod events {
    use serde::{Deserialize, Serialize};

    /// Undocumented upstream.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct WorkerCreated {
        #[serde(rename = "workerId")]
        pub worker_id: String,
        pub url: String,
        pub name: String,
    }

    impl crate::Event for WorkerCreated {
        const DOMAIN: crate::Domain = crate::Domain::Worker;
        const METHOD: &'static str = "workerCreated";
    }

    /// Undocumented upstream.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct WorkerTerminated {
        #[serde(rename = "workerId")]
        pub worker_id: String,
    }

    impl crate::Event for WorkerTerminated {
        const DOMAIN: crate::Domain = crate::Domain::Worker;
        const METHOD: &'static str = "workerTerminated";
    }

    /// Undocumented upstream.
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct DispatchMessageFromWorker {
        #[serde(rename = "workerId")]
        pub worker_id: String,
        /// JSON Inspector Protocol message (response or event) to be dispatched on the frontend.
        pub message: String,
    }

    impl crate::Event for DispatchMessageFromWorker {
        const DOMAIN: crate::Domain = crate::Domain::Worker;
        const METHOD: &'static str = "dispatchMessageFromWorker";
    }
}
